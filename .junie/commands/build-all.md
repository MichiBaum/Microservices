---
name: build-all
description: Build the entire project using Maven.
---
Execute `./mvnw clean install` in the root directory to build all services and libraries. 
Verify that all modules build successfully.
In case of failures, identify the module and provide the error summary.
