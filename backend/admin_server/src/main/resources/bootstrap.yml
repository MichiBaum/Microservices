spring:
  application:
    name: admin_server
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
      allow-override: true
      name: admin_server
      override-system-properties: true
      username: system
      password: ${CONFIG_SERVICE_PASSWORD}
