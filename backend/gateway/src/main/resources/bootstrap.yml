spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
      allow-override: true
      name: gateway
      override-system-properties: true
      username: system
      password: ${CONFIG_SERVICE_PASSWORD}
