spring:
  application:
    name: registry
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
      allow-override: true
      name: registry
      override-system-properties: true
      username: system
      password: ${CONFIG_SERVICE_PASSWORD}
