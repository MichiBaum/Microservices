server:
  shutdown: graceful
  undertow:
    url-charset: UTF-8

spring:
  lifecycle:
    timeout-per-shutdown-phase: 20s
  application:
    name: javadoc
  security:
    oauth2:
      client:
        registration:
          javadoc:
            provider: keycloak
            client-id: javadoc-service
            client-authentication-method: none
            scope: openid,profile,email
        provider:
          keycloak:
            issuer-uri: http://keycloak/auth/realms/master
  cloud:
    gcp:
      logging:
        enabled: true
      trace:
        pubsub:
          enabled: true
    kubernetes:
      reload:
        enabled: true
    discovery:
      client:
        health-indicator:
          include-description: true
  sleuth:
    sampler:
      probability: 1.0
  main:
    allow-bean-definition-overriding: true

management:
  cloudfoundry:
    enabled: false
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "*"
  endpoint:
    env:
      post:
        enabled: true
    health:
      show-details: always
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  info:
    git:
      mode: full
