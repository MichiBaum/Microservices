# Kubernetes
This files are used to deploy on Rancher(k3s) but it *should work* on k8s and other Kubernetes too.

## Order without cd

- namespace.yaml
- role-binding.yaml
- issuer.yaml 
- admin-*.yaml
- gateway-*.yaml
