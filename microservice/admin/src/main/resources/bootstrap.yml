server:
  port: 8081

spring:
  application:
    name: admin
  cloud:
    config:
      uri: http://config:8888
      enabled: true
      fail-fast: true
      allow-override: true
      name: admin
      override-system-properties: true
      retry:
        max-attempts: 20

