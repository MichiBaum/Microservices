server:
  port: 80

spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://config:8888
      enabled: true
      fail-fast: true
      allow-override: true
      name: gateway
      override-system-properties: true
      retry:
        max-attempts: 20
