/**
 * Represents a language with its name and ISO code.
 *
 * @interface Language
 * @property {string} name - The name of the language.
 * @property {string} isoCode - The ISO 639-1 code for the language.
 */
export interface Language {
  name: string;
  isoCode: string;
}
